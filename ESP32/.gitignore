.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

# Secrets: the real config holds WiFi credentials and must never be committed.
# The committed template is src/config/Config.example.h (no credentials).
src/config/Config.h
